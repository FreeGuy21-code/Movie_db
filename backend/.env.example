# Environment Variables for Movie Search Backend

# MongoDB Connection String
# For local development, you can use MongoDB Atlas or local MongoDB
# For production on Render, set this as an environment variable
MONGODB_URI=mongodb://localhost:27017/movie-search

# Server Configuration
PORT=5000
NODE_ENV=development

# Optional: OMDB API Key (if you want to seed more data)
OMDB_API_KEY=your_omdb_api_key_here
